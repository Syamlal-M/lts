import { PageContainer } from "components/layout";
import { Typography } from "components/shared-ui";

const PlanningPage = () => {
    return (
        <PageContainer title="LTS | Leave Forecast">
            <Typography>Planning Page</Typography>
        </PageContainer>

    );
};

export default PlanningPage;