import { PageContainer } from "components/layout";
import { Typography } from "components/shared-ui";

const SettingsPage = () => {
    return (
        <PageContainer title="LTS | Settings">
            <Typography>Settings Page</Typography>
        </PageContainer>
    );
};

export default SettingsPage;